# Local sidecars

This directory is the development staging location for packaged helper binaries.
`tokenbox-bridge.exe` is generated by `npm run stage:bridge` and is ignored by
Git. Build it from the sibling TokenBox project with `npm run build:bridge` or
run `npm run build:bridge:stage` from this workbench.
